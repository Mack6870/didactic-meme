{
  "name": "didactic-meme",
  "version": "1.0.0",
  "description": "Full-stack web application with React frontend and Express API backend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm install --prefix client && npm install --prefix server && npm run build --prefix client",
    "start": "NODE_ENV=production npm run start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "lint": "npm run lint --prefix client",
    "clean": "rimraf client/dist server/dist node_modules/.cache"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1"
  }
}
